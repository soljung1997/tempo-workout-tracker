//Extremely similar to category, an Id, name

export type MuscleGroup = {
    id: number;
    name: string;
    normalizedName: string;
    isDefault: boolean;
    isActive: boolean;
    createdAt: string;
    updatedAt: string;
};